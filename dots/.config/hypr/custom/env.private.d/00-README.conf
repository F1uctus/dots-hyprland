# Machine-private Hypr `env` lines live in 99-local.conf (gitignored).
#
# Example 99-local.conf (single line, no quotes on the value):
#   env = TAILSCALE_LOGIN_SERVER,https://headscale.example.com
#
# Hyprland loads this directory via a glob; this file only documents the pattern.
